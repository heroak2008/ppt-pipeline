> **先不急着自动生成 PPT，而是把“素材 PPT、规范 PPT、优秀范例 PPT”解析成可检索、可复用、可人工审核的资产库。**

这一步是后续“按规范生成”和“匹配优秀范例风格”的基础。
 但目前并没有一个开源工具能对任意复杂企业 PPT 做到“一键完美解析、自动分类、自动变成可用模板库”。更现实的做法是组合现成解析库，建设一个**PPT 资产提取流水线**。

------

# 推荐结论

## 私域 / 开源优先方案

| 能力                | 推荐工具                                                    | 是否开源  | 用途                                                  |
| ------------------- | ----------------------------------------------------------- | --------- | ----------------------------------------------------- |
| PPTX 基础解析       | [python-pptx](https://python-pptx.readthedocs.io/)          | 是        | 读取幻灯片、文本、图片、形状、表格、坐标、字体等      |
| 深度素材提取        | `zipfile` + OOXML/XML 解析                                  | 是        | 提取主题、母版、版式、背景图、媒体、嵌入文件等        |
| PPT 页面渲染        | LibreOffice Headless                                        | 是        | 批量转 PDF/PNG，产出缩略图预览                        |
| 浏览器高保真预览    | [pptx-renderer](https://github.com/aiden0z/pptx-renderer)   | 是        | 将 PPTX 解析并渲染为 HTML/SVG DOM，适合资产库页面预览 |
| 图像去重 / 相似检索 | OpenCV + `imagehash` + CLIP/私域视觉模型                    | 是/可私域 | 合并重复 Logo、图标、背景图，做相似页面检索           |
| 元数据与检索        | PostgreSQL + pgvector 或 Qdrant                             | 是        | 保存资产、标签、文字、向量及检索索引                  |
| 高保真企业级解析    | [Aspose.Slides](https://docs.aspose.com/slides/python-net/) | 否，商业  | 复杂图表、SmartArt、母版、渲染、提取方面更稳          |

`python-pptx` 是首选基础库：它可读取、创建和更新 `.pptx`，并可访问文本、图片、表格、形状等 PowerPoint 对象。 [ref:11,14]
 如果企业 PPT 中存在大量复杂母版、SmartArt、嵌入对象、图表或特殊组合图形，建议评估 Aspose.Slides；它对幻灯片、形状、图表、多媒体和嵌入对象的对象模型覆盖更完整，并提供图像提取能力。 [ref:31,37]

------

# 你应该建立的不是“一个文件夹”，而是四类资产库

你提供的三类文件，应分别提炼成不同资产。

```text
PPT 文件输入
├── ① 多元素素材 PPT
├── ② PPT 规范示例
└── ③ 优秀 PPT 范例
          ↓
资产解析与人工审核
          ↓
├── A. 原始素材库
├── B. 品牌/规范库
├── C. 页面模板与版式库
└── D. 样例知识与检索库
```

------

## A. 原始素材库：来自“多种元素素材的 PPT”

这类 PPT 不要只导出为图片；应保留**原始对象、预览图和结构信息**。

建议至少提取以下资产：

| 资产类型   | 例子                         | 提取方式                             | 后续用途             |
| ---------- | ---------------------------- | ------------------------------------ | -------------------- |
| 图片       | Logo、照片、背景、纹理、插图 | 导出原始 PNG/JPEG/SVG/EMF            | 生成 PPT 时引用      |
| 图标       | 线性图标、业务图标、箭头     | 尽量保留 SVG/EMF；同时生成 PNG 预览  | 图标检索与复用       |
| 图形组件   | 卡片、标签、色块、流程节点   | 提取形状 XML、坐标、填充、描边、文字 | 重建为 PPT 组件      |
| 图表组件   | KPI 卡、柱线图、漏斗、瀑布图 | 提取图表类型、数据、色彩、布局       | 建立可填充图表模板   |
| 表格组件   | 对比表、项目表、风险表       | 提取行列、样式、文字格式             | 作为表格模板         |
| 背景与装饰 | 页眉、页脚、角标、水印、纹理 | 从母版、背景、图片中提取             | 统一页面视觉         |
| 字体与色板 | 品牌主色、辅助色、字号层级   | 从主题、母版、样例页统计             | 建设计规范           |
| 动画/媒体  | 视频、音频、动画效果         | 记录存在性、提取原文件               | 通常第一期不自动复用 |

**注意：**
 素材 PPT 中的组合图形、SmartArt、图表、图标字体字形等，不能简单等同于图片。它们需要记录“对象类型”和“可编辑性”。对复杂对象，第一期建议生成一个**可视化预览 + 原始页/原始对象引用**，不强行拆解重建。

------

## B. 规范库：来自“用 PPT 举例的 PPT 规范”

很多企业规范并不是 Word 文档，而是“**对的样子**”写在示例 PPT 里。

解析后要形成机器可读的 `design-system.json`，并保留规则与示例页之间的关联。

例如：

```json
{
  "deck": {
    "page_size": "16:9",
    "width_inches": 13.333,
    "height_inches": 7.5
  },
  "colors": {
    "primary": ["#003A70"],
    "secondary": ["#00A6A6", "#5A6B7A", "#E8EDF2"],
    "forbidden": ["#FF0000"]
  },
  "typography": {
    "title": {
      "font": "企业指定中文字体",
      "font_size_pt": 26,
      "bold": true,
      "max_chars": 26
    },
    "body": {
      "font": "企业指定中文字体",
      "font_size_pt": 11,
      "minimum_font_size_pt": 9
    }
  },
  "common_rules": {
    "title_should_be_conclusion_first": true,
    "footer_required": true,
    "page_number_required": true,
    "logo_required": true,
    "allow_external_images": false
  }
}
```

这里有一个关键点：

- **自动程序可以提取“实际用了什么”**：字体、色值、位置、大小、页边距；
- **私域 LLM 或人工要补充“为什么这样用”**：例如标题必须结论先行、哪些页面适用于管理层、哪些图表不能使用。

所以，规范 PPT 需要经过一次“**自动提取 + 人工确认 + 规则结构化**”。

------

## C. 页面模板与版式库：来自“优秀 PPT 范例”

优秀范例最有价值的不是它里面的某张图片，而是它展示了：

- 什么议题应该用什么页面结构；
- 标题、核心结论、数据图和说明文字如何分工；
- 如何控制一页的信息密度；
- 章节之间如何过渡；
- 什么内容应采用三栏、对比表、时间线、矩阵或数据图。

建议对每页建立一条“页面资产记录”。

```json
{
  "asset_id": "sample_2026_strategy_008",
  "source_file": "2026战略规划优秀范例.pptx",
  "slide_no": 8,
  "slide_type": "three-pillars",
  "template_id": "three-pillars-v2",
  "tags": [
    "战略汇报",
    "管理层",
    "增长举措",
    "三栏结构",
    "结论先行"
  ],
  "thumbnail_path": "thumbnails/sample_2026_strategy_008.png",
  "layout": {
    "title_area": [0.65, 0.42, 11.9, 0.55],
    "headline_area": [0.65, 1.1, 11.5, 0.45],
    "content_areas": [
      [0.65, 2.0, 3.85, 3.9],
      [4.75, 2.0, 3.85, 3.9],
      [8.85, 2.0, 3.85, 3.9]
    ]
  },
  "capacity": {
    "title_max_chars": 26,
    "headline_max_chars": 44,
    "column_count": 3,
    "column_title_max_chars": 10,
    "column_body_max_chars": 60
  },
  "approved": true,
  "quality_score": 5
}
```

之后，当 AI 需要做“增长策略”页面时，检索到的不是一段描述，而是：

- 一张缩略图；
- 它的页面语义；
- 可容纳多少内容；
- 对应的模板 ID；
- 使用过的真实优秀案例。

------

## D. 检索库：让后续 AI 找到“最像的参考页”

未来输入一个大纲时，AI 不应该看到全部 1000 页范例，而应先检索。

例如用户输入：

> “需要展示未来三年实施路径、关键里程碑和责任部门。”

系统应找到：

```text
slide_type: roadmap
tags: 时间线 / 路线图 / 项目计划 / 三阶段 / 责任分工
候选模板：
- roadmap-horizontal-v1
- roadmap-three-phase-v2
- roadmap-milestone-v3
```

检索依据可以组合：

1. **文字检索**：标题、备注、页内文字、人工标签；
2. **结构检索**：有几栏、是否含图表、图表类型、文本框数量；
3. **视觉检索**：页面缩略图的图像向量；
4. **语义检索**：私域 LLM/Embedding 对页面用途的理解；
5. **业务标签**：战略、经营、项目、销售、投标、客户汇报等。

------

# 一个可实际运行的资产提取流水线

## 第一步：原文件归档和基本信息扫描

对每个文件生成唯一 ID，保留只读原件。

```text
/raw-pptx/
  ├── source-001.pptx
  ├── source-002.pptx
  └── source-003.pptx

/asset-library/
  ├── thumbnails/
  ├── extracted-media/
  ├── extracted-svg/
  ├── slide-json/
  ├── template-previews/
  └── reports/
```

记录：

- 文件名、来源类别、创建/导入时间；
- 页数、页面尺寸、版本；
- 是否含母版、图表、视频、音频、嵌入 Excel；
- 是否加密、是否含宏；
- 文件哈希值，用于去重和审计。

------

## 第二步：解包 PPTX，提取原始媒体和主题

`.pptx` 本质是 ZIP 格式的 Office Open XML 包。

可直接提取：

```text
ppt/media/                图片、视频等媒体
ppt/theme/                主题颜色与字体
ppt/slideMasters/         幻灯片母版
ppt/slideLayouts/         版式定义
ppt/slides/               每页的 XML 内容
ppt/charts/               图表定义
ppt/embeddings/           内嵌的 Excel、OLE 对象
ppt/notesSlides/          演讲者备注
```

这一步要优先保留：

- 原图原格式；
- 原始 SVG、EMF、WMF，而不是只转 PNG；
- 内嵌 Excel；
- 母版与主题 XML；
- 形状、图表、表格的 XML。

Microsoft 也明确说明，PPTX 可通过将扩展名改为 `.zip` 的方式提取其中图片、视频等对象；程序化方案本质上就是对这一结构做更系统的解析。 [ref:6]

------

## 第三步：页级结构解析

使用 `python-pptx` 读取每一页及其 shape：

- 文本框、占位符、标题；
- 图片；
- 自选图形；
- 组合对象；
- 表格；
- 图表；
- SmartArt / OLE / 媒体的存在性；
- 对象坐标、尺寸、旋转；
- 填充色、线条、透明度；
- 字体、字号、字重、对齐、段落；
- 页面备注；
- 层级顺序（z-order）。

`python-pptx` 适合作为这层的主解析器；如果你们是 Java 技术栈，也可以使用 Apache POI 的 XSLF API 遍历每张幻灯片的形状。 [ref:11,21]

------

## 第四步：渲染缩略图与视觉资产

每份 PPT、每一页 PPT 都应生成：

```text
原文件
  ↓
PDF
  ↓
每页 PNG（建议 1600px 宽以上）
  ↓
缩略图（如 480px 宽）
  ↓
视觉向量 / 相似度索引
```

用途：

- 在资产管理 Web 界面快速浏览；
- 作为 AI 的视觉参考；
- 做相似页面搜索；
- 对比同一页面模板的不同版本；
- 后续进行文字截断、重叠、低清图片等质检。

如果希望在浏览器中更细粒度地展示并分析 PPT 内容，可以关注 `pptx-renderer`：它尝试将 PPTX 解析并在浏览器中渲染为 HTML/SVG DOM，适合作为预览和页面结构可视化层，但不建议将它作为企业资产提取的唯一解析引擎。 [ref:1]

------

# 推荐的最小技术架构

```text
                 ┌────────────────────────────┐
                 │ 内网文件目录 / MinIO / NAS  │
                 └──────────────┬─────────────┘
                                │
                                ▼
                 ┌────────────────────────────┐
                 │ PPT Asset Extractor        │
                 │ Python                     │
                 │ - zipfile / lxml           │
                 │ - python-pptx              │
                 │ - OpenCV / imagehash       │
                 └──────────────┬─────────────┘
                                │
            ┌───────────────────┼────────────────────┐
            ▼                   ▼                    ▼
   ┌────────────────┐  ┌────────────────┐  ┌────────────────┐
   │ 原始媒体资产库 │  │ 页面结构 JSON  │  │ 页面 PNG 缩略图 │
   │ PNG/SVG/EMF等  │  │ 文本/形状/样式 │  │ 预览/视觉检索   │
   └────────────────┘  └────────────────┘  └────────────────┘
            │                   │                    │
            └───────────────────┴────────────────────┘
                                ▼
                 ┌────────────────────────────┐
                 │ PostgreSQL + pgvector      │
                 │ 或 Qdrant                  │
                 │ 元数据、标签、向量、版本   │
                 └──────────────┬─────────────┘
                                ▼
                 ┌────────────────────────────┐
                 │ 资产审核与检索 Web 界面    │
                 │ 选优、打标签、标模板、版本 │
                 └────────────────────────────┘
```

------

# 建议的工具组合：按优先级选择

## 方案 1：开源优先、适合先做 PoC

```text
Python 3.11+
├── python-pptx：页面/文本/形状/图片等基础解析
├── zipfile + lxml：OOXML、母版、主题、背景、媒体深度提取
├── LibreOffice Headless：PPTX 转 PDF / PNG
├── PyMuPDF：PDF 转 PNG、页面预览处理
├── Pillow + imagehash：图片去重
├── OpenCV：基础视觉特征与质检
├── PostgreSQL + pgvector：资产元数据和语义/视觉检索
└── MinIO / NAS：原文件与素材文件存储
```

**适合：** 首期验证、私域隔离环境、预算有限、有 Python 开发或私域 AI 编程能力。

------

## 方案 2：高保真企业方案

```text
Python 或 .NET / Java
├── Aspose.Slides：高保真解析、编辑、渲染、提取
├── PostgreSQL + pgvector / Qdrant
├── MinIO / 企业对象存储
├── 内网视觉模型 / Embedding 模型
└── Web 审核台
```

**适合：** 范例 PPT 有大量复杂母版、图表、OLE/嵌入 Excel、SmartArt、组合对象，且对兼容性和稳定性要求高。

代价是 Aspose.Slides 需要商业授权；但在“不要破坏企业现有优秀 PPT”这个任务上，往往比完全自研 XML 解析更省总成本。

------

# 一个重要判断：哪些对象可以自动资产化

| PPT 对象               | 自动提取难度 | 建议第一期处理方式                    |
| ---------------------- | ------------ | ------------------------------------- |
| PNG / JPG 图片         | 低           | 原样导出、去重、打标签                |
| SVG / EMF / WMF 图标   | 中           | 原件保留 + 生成 PNG 预览              |
| 普通文本框             | 低           | 提取文本、字体、位置、样式            |
| 色块、线条、矩形、箭头 | 低           | 记录为可重建组件                      |
| 普通表格               | 中           | 提取结构、样式、容量                  |
| 常规图表               | 中           | 提取图表类型、系列、色彩、数据        |
| 组合形状               | 中高         | 保留为页面组件；不要急于拆分          |
| SmartArt               | 高           | 预览 + 原始模板页引用；后期专项支持   |
| 动画和切换             | 高           | 仅记录，不作为第一期自动复用对象      |
| 嵌入 Excel / OLE       | 中高         | 原件提取、建立关联；按需人工审核      |
| 复杂母版与版式继承     | 高           | 提取并保存；模板复用时优先保留原 PPTX |

------

# 不要遗漏“人工审核台”

自动解析只解决“提取”，不能自动保证“哪些东西值得复用”。

建议资产库有一个简单审核界面，支持：

- 按文件、页面、素材类型浏览；
- 看每页缩略图和提取出的结构；
- 标记：`优秀` / `可复用` / `仅参考` / `禁止使用`；
- 标注页面类型：封面、摘要、趋势图、对比表、路线图等；
- 给素材添加标签：Logo、人物、产品、行业、场景、颜色、风格；
- 标记版权/保密等级和可使用范围；
- 合并重复素材；
- 指定某页为“正式模板候选”；
- 为模板填写内容容量限制。

没有这一步，资产库最后很容易变成“能搜到一堆图片和 PPT 页面，但不知道该用哪个”的资料仓库。

------

# 给你的私域 AI 的第一期实施任务

你可以直接将下面内容交给私域 AI 或开发人员：

```text
目标：
构建完全在内网运行的 PPT 资产提取与检索系统。
输入包括：
1. 多种元素素材 PPT；
2. 以 PPT 形式编写的规范说明；
3. 优秀 PPT 范例。
第一期只做资产解析、分类、预览、检索与人工审核，不生成新的 PPT。

技术要求：
- Python 3.11+
- python-pptx 用于 PPTX 页面、文本、图片、形状、表格、图表的基础解析
- zipfile + lxml 用于深度读取 PPTX 的 OOXML 包：
  ppt/media、ppt/theme、ppt/slideMasters、ppt/slideLayouts、
  ppt/slides、ppt/charts、ppt/embeddings、ppt/notesSlides
- 使用 LibreOffice Headless 将 PPTX 转 PDF，再生成每页 PNG 和缩略图
- 使用 PostgreSQL 保存文件、页面、对象、模板和审核元数据
- 使用 MinIO 或内网 NAS 保存原始 PPTX、提取媒体、预览 PNG
- 使用 pgvector 或 Qdrant 保存页面文本、页面标签、缩略图视觉向量
- 所有处理必须离线/内网完成，禁止任何外部 API 调用

功能要求：
1. 导入 PPTX 后生成文件级、页面级、对象级 JSON。
2. 对每一页提取：
   页码、尺寸、母版/版式、文本、形状类型、坐标、尺寸、层级、
   填充色、线条、字体、字号、图片关系、表格、图表和备注。
3. 导出所有 ppt/media 下的原始图片和媒体，同时保留原始格式。
4. 解析主题色、主题字体、母版、页眉页脚、Logo 和背景元素。
5. 生成每页 PNG、缩略图和可用于视觉相似检索的向量。
6. 对素材使用 SHA-256 和感知哈希进行去重。
7. 支持人工给文件、页面和素材打标签、标记质量等级和可用范围。
8. 支持将优秀页面标为：
   - 页面类型；
   - 模板候选；
   - 内容容量；
   - 可复用组件；
   - 是否允许自动生成系统使用。
9. 输出资产盘点报告：
   - 文件数量、总页数；
   - 图片/图标/图表/表格/SmartArt/嵌入对象数量；
   - 字体、色板、Logo 使用统计；
   - 高复用页面候选；
   - 解析异常、未支持对象和人工复核清单。
```

------

## 最后给你一个务实的启动建议

第一批不要一次性解析全部历史 PPT。建议先选：

- **1 份素材 PPT**；
- **1 份规范 PPT**；
- **5–10 份公认优秀的范例 PPT**。

完成资产提取后，人工从中挑出：

- 约 **30–50 个高价值素材**；
- 约 **8–12 个高频页面类型**；
- 约 **10–20 个可作为正式模板候选的优秀页面**；
- 一份确认过的字体、色板、页脚和标题规范。

这样，你就拥有了后续自动生成系统真正需要的第一版“设计资产底座”。